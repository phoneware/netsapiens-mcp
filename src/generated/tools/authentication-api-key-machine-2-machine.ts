// Auto-generated by scripts/generate-tools.ts — DO NOT EDIT
import type { ToolDefinition, GenericApiClient } from '../types.js';
import type { CallToolResult } from '@modelcontextprotocol/sdk/types.js';

export const read_my_apikey: ToolDefinition = {
  schema: {
    name: 'read_my_apikey',
    description: 'Read API Key info on your API Key — This parameter-less action will return the information on the API key being used for the request giving confirmation on access levels allowed. — [GET /apikeys/~]',
    inputSchema: {
          "type": "object",
          "properties": {}
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/apikeys/~',
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const read_apikeys: ToolDefinition = {
  schema: {
    name: 'read_apikeys',
    description: 'Read API Keys under your account — This action will show apikeys that have been generated by your current APIkey and that you have access to update or revoke as needed.   > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [GET /apikeys]',
    inputSchema: {
          "type": "object",
          "properties": {}
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/apikeys',
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const read_apikey: ToolDefinition = {
  schema: {
    name: 'read_apikey',
    description: 'Read Info on specific APIKey via Key ID — This action will show apikeys that have been generated by your current APIkey and that you have access to update or revoke as needed.   > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [GET /apikeys/{key_id}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "key_id": {
                      "type": "string",
                      "description": "key_id"
                }
          },
          "required": [
                "key_id"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['key_id'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/apikeys/{key_id}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const update_apikey: ToolDefinition = {
  schema: {
    name: 'update_apikey',
    description: 'Update API Key — This is a limited action and will require special access to create API keys. The Update is even more limited only allowing the change of the description and IP restrictions. You will not be able to change the scope, access rights or any premissions. A new apikey would need to be created in those cases. — [PUT /apikeys/{key_id}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "key_id": {
                      "type": "string",
                      "description": "key_id"
                },
                "description": {
                      "type": "string",
                      "description": "This is the main description/name for the API key that should describe its purpose for tracking and understanding its intended use."
                },
                "ip-address": {
                      "type": "string",
                      "description": "Optional IP restrictions for API to require access from matching IP or subnets. Multipe ips/subnets can be provided comma seperated"
                },
                "allowed-models": {
                      "type": "string",
                      "description": "This is an optional limitation to the ability of this apikey. The default is  * or all models, but can be set to a specific model or array of models allowing specific access. The \"models\" are linked to objects as defined in the scope definition and might have varies terminolofy from the api itself."
                }
          },
          "required": [
                "key_id",
                "description"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['key_id'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['description', 'ip-address', 'allowed-models'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/apikeys/{key_id}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const revoke_apikey: ToolDefinition = {
  schema: {
    name: 'revoke_apikey',
    description: 'Revoke API Key — This is a limited action and will require special access to revoke API keys. Revoking the apikey will remove the key from the DB and any Cache stoping access immediately. — [DELETE /apikeys/{key_id}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "key_id": {
                      "type": "string",
                      "description": "key_id"
                }
          },
          "required": [
                "key_id"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['key_id'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'DELETE',
      pathTemplate: '/apikeys/{key_id}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const create_apikey: ToolDefinition = {
  schema: {
    name: 'create_apikey',
    description: 'Create API Key — This is a limited action and will require special access to create API keys. — [POST /apikeys]',
    inputSchema: {
          "type": "object",
          "properties": {
                "description": {
                      "type": "string",
                      "description": "This is the main description/name for the API key that should describe its purpose for tracking and understanding its intended use."
                },
                "reseller": {
                      "type": "string",
                      "description": "This is the reseller that will limit the API key to access the resellers data set. Default: \"*\"",
                      "default": "*"
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account."
                },
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. ~ and * can be used alone in special cases to mean My Domain (~) and All Domains (\\*)."
                },
                "user-scope": {
                      "type": "string",
                      "description": "This is the scope that will be be used to enforce permissions for this API key and access. Allowed values: \"Basic User\", \"Simple User\", \"Advanced User\", \"Call Center Agent\", \"Call Center Supervisor\", \"Office Manager\", \"Reseller\", \"Super User\", \"NDP\"",
                      "enum": [
                            "Basic User",
                            "Simple User",
                            "Advanced User",
                            "Call Center Agent",
                            "Call Center Supervisor",
                            "Office Manager",
                            "Reseller",
                            "Super User",
                            "NDP"
                      ]
                },
                "ip-address": {
                      "type": "string",
                      "description": "Optional IP restrictions for API to require access from matching IP or subnets. Multipe ips/subnets can be provided comma seperated"
                },
                "readonly": {
                      "type": "string",
                      "description": "If enabled it will limit any get Scope to a read only verion providing only GET acces to the read actions. POST, PUT and DELETE will be rejected. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "can-create-keys": {
                      "type": "string",
                      "description": "If enabled it will allow this key to make keys for lesser scopes then itself. It will not allow creations of scopes same or higher access level. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "allowed-models": {
                      "type": "string",
                      "description": "This is an optional limitation to the ability of this apikey. The default is  * or all models, but can be set to a specific model or array of models allowing specific access. The \"models\" are linked to objects as defined in the scope definition and might have varies terminolofy from the api itself."
                },
                "created-datetime": {
                      "type": "string",
                      "description": "The datetime that the key was created."
                },
                "lastused-datetime": {
                      "type": "string",
                      "description": "The datetime of the last time the apikey was used for API access. The timestamp is acurrate up to the hour to avoid overhead in tracking high volume requests"
                }
          },
          "required": [
                "description",
                "reseller",
                "domain"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['description', 'reseller', 'user', 'domain', 'user-scope', 'ip-address', 'readonly', 'can-create-keys', 'allowed-models', 'created-datetime', 'lastused-datetime'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/apikeys',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};
