// Auto-generated by scripts/generate-tools.ts — DO NOT EDIT
import type { ToolDefinition, GenericApiClient } from '../types.js';
import type { CallToolResult } from '@modelcontextprotocol/sdk/types.js';

export const post_auth_code: ToolDefinition = {
  schema: {
    name: 'post_auth_code',
    description: 'Get Access Token From Auth Code — [POST /authCode]',
    inputSchema: {
          "type": "object",
          "properties": {
                "grant_type": {
                      "type": "string",
                      "description": "This is the type of authentication request being made. 'mfa' would be needed in the second request with MFA passcode. Allowed values: \"mfa\", \"password\", \"refresh_token\". Default: \"mfa\"",
                      "enum": [
                            "mfa",
                            "password",
                            "refresh_token"
                      ],
                      "default": "mfa"
                },
                "client_id": {
                      "type": "string",
                      "description": "This is the client_id provided to the developer for accessing the api. Will match with the client_secret"
                },
                "client_secret": {
                      "type": "string",
                      "description": "This is the client_secret provided to the developer for accessing the api. Will match with the client_id"
                },
                "username": {
                      "type": "string",
                      "description": "This is the username and logic for the user. This may look like a uid (user@domain) or could be something else if setup diffferent including a email address potentially."
                },
                "password": {
                      "type": "string",
                      "description": "This is the password for the user requesting access."
                }
          },
          "required": [
                "grant_type",
                "client_id",
                "client_secret",
                "username",
                "password"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['grant_type', 'client_id', 'client_secret', 'username', 'password'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/authCode',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_tokens_1: ToolDefinition = {
  schema: {
    name: 'post_tokens_1',
    description: 'Get Access Token From Refresh — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [POST /tokens]',
    inputSchema: {
          "type": "object",
          "properties": {
                "grant_type": {
                      "type": "string",
                      "description": "This is the type of authentication request being made. 'password' is most common if using username and password from end user to grant access. Allowed values: \"password\", \"refresh_token\", \"mfa\". Default: \"password\"",
                      "enum": [
                            "password",
                            "refresh_token",
                            "mfa"
                      ],
                      "default": "password"
                },
                "client_id": {
                      "type": "string",
                      "description": "This is the client_id provided to the developer for accessing the api. Will match with the client_secret"
                },
                "client_secret": {
                      "type": "string",
                      "description": "This is the client_secret provided to the developer for accessing the api. Will match with the client_id"
                },
                "refresh_token": {
                      "type": "string",
                      "description": "refresh_token"
                }
          },
          "required": [
                "grant_type",
                "client_id",
                "client_secret",
                "refresh_token"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['grant_type', 'client_id', 'client_secret', 'refresh_token'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/tokens',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_tokens_2: ToolDefinition = {
  schema: {
    name: 'post_tokens_2',
    description: 'Get Access Token after MFA include Passcode — This API will accept username and password along with a passcode generated via a authenticator application and generate a Access token — [POST /tokens]',
    inputSchema: {
          "type": "object",
          "properties": {
                "grant_type": {
                      "type": "string",
                      "description": "This is the type of authentication request being made. 'mfa' would be needed in the second request with MFA passcode. Allowed values: \"mfa\", \"password\", \"refresh_token\". Default: \"mfa\"",
                      "enum": [
                            "mfa",
                            "password",
                            "refresh_token"
                      ],
                      "default": "mfa"
                },
                "client_id": {
                      "type": "string",
                      "description": "This is the client_id provided to the developer for accessing the api. Will match with the client_secret"
                },
                "client_secret": {
                      "type": "string",
                      "description": "This is the client_secret provided to the developer for accessing the api. Will match with the client_id"
                },
                "username": {
                      "type": "string",
                      "description": "This is the username and logic for the user. This may look like a uid (user@domain) or could be something else if setup diffferent including a email address potentially."
                },
                "password": {
                      "type": "string",
                      "description": "This is the password for the user requesting access."
                },
                "mfa_type": {
                      "type": "string",
                      "description": "This value will be provided in response to a auth request if MFA is needed. Move value from that token to this request. Likely will be 'authenticator'. Default: \"authenticator\"",
                      "default": "authenticator"
                },
                "mfa_vendor": {
                      "type": "string",
                      "description": "This value will be provided in response to a auth request if MFA is needed. Move value from that token to this request.  Likely will be 'google'. Default: \"google\"",
                      "default": "google"
                },
                "ns_id_type": {
                      "type": "string",
                      "description": "Will likely be subscriber as the value here to defierentiate between authenticator for api/portal and mfa with admin ui. Allowed values: \"subscriber\", \"level1\". Default: \"subscriber\"",
                      "enum": [
                            "subscriber",
                            "level1"
                      ],
                      "default": "subscriber"
                },
                "passcode": {
                      "type": "string",
                      "description": "This will be the 6 digit numberic passcode provided by a"
                },
                "access_token": {
                      "type": "string",
                      "description": "This will be the access_token or jwt that was given with the scope of mfa_required"
                }
          },
          "required": [
                "grant_type",
                "client_id",
                "client_secret",
                "username",
                "password",
                "mfa_type",
                "mfa_vendor",
                "ns_id_type",
                "passcode",
                "access_token"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['grant_type', 'client_id', 'client_secret', 'username', 'password', 'mfa_type', 'mfa_vendor', 'ns_id_type', 'passcode', 'access_token'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/tokens',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_tokens_3: ToolDefinition = {
  schema: {
    name: 'post_tokens_3',
    description: 'Get Access Token after MFA request — This API will accept username and password along with a passcode generated via a authenticator application and generate a Access token — [POST /tokens]',
    inputSchema: {
          "type": "object",
          "properties": {
                "grant_type": {
                      "type": "string",
                      "description": "This is the type of authentication request being made. 'mfa' would be needed in the second request with MFA passcode. Allowed values: \"mfa\", \"password\", \"refresh_token\". Default: \"mfa\"",
                      "enum": [
                            "mfa",
                            "password",
                            "refresh_token"
                      ],
                      "default": "mfa"
                },
                "client_id": {
                      "type": "string",
                      "description": "This is the client_id provided to the developer for accessing the api. Will match with the client_secret"
                },
                "client_secret": {
                      "type": "string",
                      "description": "This is the client_secret provided to the developer for accessing the api. Will match with the client_id"
                },
                "username": {
                      "type": "string",
                      "description": "This is the username and logic for the user. This may look like a uid (user@domain) or could be something else if setup diffferent including a email address potentially."
                },
                "password": {
                      "type": "string",
                      "description": "This is the password for the user requesting access."
                },
                "mfa_type": {
                      "type": "string",
                      "description": "This value will be provided in response to a auth request if MFA is needed. Move value from that token to this request. Likely will be 'authenticator'. Default: \"authenticator\"",
                      "default": "authenticator"
                },
                "mfa_vendor": {
                      "type": "string",
                      "description": "This value will be provided in response to a auth request if MFA is needed. Move value from that token to this request.  Likely will be 'google'. Default: \"google\"",
                      "default": "google"
                },
                "ns_id_type": {
                      "type": "string",
                      "description": "Will likely be subscriber as the value here to defierentiate between authenticator for api/portal and mfa with admin ui. Allowed values: \"subscriber\", \"level1\". Default: \"subscriber\"",
                      "enum": [
                            "subscriber",
                            "level1"
                      ],
                      "default": "subscriber"
                },
                "passcode": {
                      "type": "string",
                      "description": "This will be the 6 digit numberic passcode provided by a"
                },
                "access_token": {
                      "type": "string",
                      "description": "This will be the access_token or jwt that was given with the scope of mfa_required"
                }
          },
          "required": [
                "grant_type",
                "client_id",
                "client_secret",
                "username",
                "password",
                "mfa_type",
                "mfa_vendor",
                "ns_id_type",
                "passcode",
                "access_token"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['grant_type', 'client_id', 'client_secret', 'username', 'password', 'mfa_type', 'mfa_vendor', 'ns_id_type', 'passcode', 'access_token'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/tokens',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_sso_enroll: ToolDefinition = {
  schema: {
    name: 'post_sso_enroll',
    description: 'SSO Enroll — [POST /ssoEnroll]',
    inputSchema: {
          "type": "object",
          "properties": {
                "grant_type": {
                      "type": "string",
                      "description": "grant_type"
                },
                "client_id": {
                      "type": "string",
                      "description": "client_id"
                },
                "client_secret": {
                      "type": "string",
                      "description": "client_secret"
                },
                "username": {
                      "type": "string",
                      "description": "username"
                },
                "password": {
                      "type": "string",
                      "description": "password"
                },
                "vendor": {
                      "type": "string",
                      "description": "vendor"
                },
                "idtoken": {
                      "type": "string",
                      "description": "idtoken"
                },
                "scope": {
                      "type": "string",
                      "description": "scope"
                }
          },
          "required": [
                "grant_type",
                "client_id",
                "client_secret",
                "username",
                "password",
                "vendor",
                "idtoken",
                "scope"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['grant_type', 'client_id', 'client_secret', 'username', 'password', 'vendor', 'idtoken', 'scope'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/ssoEnroll',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};
