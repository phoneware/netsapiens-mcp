// Auto-generated by scripts/generate-tools.ts — DO NOT EDIT
import type { ToolDefinition, GenericApiClient } from '../types.js';
import type { CallToolResult } from '@modelcontextprotocol/sdk/types.js';

export const get_certificates: ToolDefinition = {
  schema: {
    name: 'get_certificates',
    description: 'Read SSL certificates for CertManager — [GET /certificates]',
    inputSchema: {
          "type": "object",
          "properties": {}
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/certificates',
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_certificates: ToolDefinition = {
  schema: {
    name: 'post_certificates',
    description: 'Create SSL certificate for CertManager — [POST /certificates]',
    inputSchema: {
          "type": "object",
          "properties": {
                "common-name": {
                      "type": "string",
                      "description": "This is the main hostname for the certficate. This must be a FQDN and much post via CNAME or A record to a ip(s) of the cluster"
                },
                "subject-alternative-name": {
                      "type": "string",
                      "description": "These are alterntive names that can be included in the certificate, each one comma seperated must be valid and pointed at the cluster"
                },
                "reseller": {
                      "type": "string",
                      "description": "This is the reseller that owns and has access to the certificate"
                },
                "account-email-address": {
                      "type": "string",
                      "description": "This is the main email address associated with the certificate request, it will be used to auto create an account with LetsEncrypt"
                },
                "challenge-type": {
                      "type": "string",
                      "description": "http-01 or dns-01, for most all requests it will be http-01 and you can leave off requests. Allowed values: \"http-01\", \"dns-01\"",
                      "enum": [
                            "http-01",
                            "dns-01"
                      ]
                },
                "default-ssl-cert": {
                      "type": "string",
                      "description": "Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "replicate-ssl-cert": {
                      "type": "string",
                      "description": "For most cases this will be \"yes\" or \"ndp\" with the later being used to create ndp/endpoint specific certifcates replicated to those servers only. Allowed values: \"yes\", \"ndp\", \"no\". Default: \"yes\"",
                      "enum": [
                            "yes",
                            "ndp",
                            "no"
                      ],
                      "default": "yes"
                }
          },
          "required": [
                "common-name",
                "reseller",
                "account-email-address"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['common-name', 'subject-alternative-name', 'reseller', 'account-email-address', 'challenge-type', 'default-ssl-cert', 'replicate-ssl-cert'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/certificates',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const get_certificates_by_name: ToolDefinition = {
  schema: {
    name: 'get_certificates_by_name',
    description: 'Read SSL certificate by Common Name — [GET /certificates/{name}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "name": {
                      "type": "string",
                      "description": "name"
                }
          },
          "required": [
                "name"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['name'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/certificates/{name}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const put_certificates_by_name: ToolDefinition = {
  schema: {
    name: 'put_certificates_by_name',
    description: 'Update SSL certificate for CertManager — [PUT /certificates/{name}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "name": {
                      "type": "string",
                      "description": "name"
                },
                "subject-alternative-name": {
                      "type": "string",
                      "description": "These are alterntive names that can be included in the certificate, each one comma seperated must be valid and pointed at the cluster"
                },
                "reseller": {
                      "type": "string",
                      "description": "This is the reseller that owns and has access to the certificate"
                },
                "account-email-address": {
                      "type": "string",
                      "description": "This is the main email address associated with the certificate request, it will be used to auto create an account with LetsEncrypt"
                },
                "expiration-datetime": {
                      "type": "string",
                      "description": "The datetime the certificate will expire. Only valid option for 'update' is 0 which will reset the expires time and cause the certificate to be regenerated as soon as possible."
                }
          },
          "required": [
                "name",
                "reseller",
                "account-email-address"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['name'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['subject-alternative-name', 'reseller', 'account-email-address', 'expiration-datetime'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/certificates/{name}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const delete_certificates_by_name: ToolDefinition = {
  schema: {
    name: 'delete_certificates_by_name',
    description: 'Delete SSL certificate for CertManager — [DELETE /certificates/{name}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "name": {
                      "type": "string",
                      "description": "name"
                }
          },
          "required": [
                "name"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['name'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'DELETE',
      pathTemplate: '/certificates/{name}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};
