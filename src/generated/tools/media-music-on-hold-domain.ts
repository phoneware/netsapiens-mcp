// Auto-generated by scripts/generate-tools.ts — DO NOT EDIT
import type { ToolDefinition, GenericApiClient } from '../types.js';
import type { CallToolResult } from '@modelcontextprotocol/sdk/types.js';

export const read_moh_domain: ToolDefinition = {
  schema: {
    name: 'read_moh_domain',
    description: 'Read MOH for Domain — [GET /domains/{domain}/moh]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                }
          },
          "required": [
                "domain"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/domains/{domain}/moh',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const create_moh_domain_tts: ToolDefinition = {
  schema: {
    name: 'create_moh_domain_tts',
    description: 'Create a new MOH for Domain from TTS — [POST /domains/{domain}/moh]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "synchronous": {
                      "type": "string",
                      "description": "When synchronous is requested with \"yes\" the response will be a 200 on success and will contain a valid JSON representiation of the new resource. If no or left off request a 202 \"Accepted\" will be given in a success case and this can be considered a success. Synchronous responses will be a little slower as the API will process the geo replication request and wait till the local copy has been written and can be read back berfore sending a response. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "index": {
                      "type": "number",
                      "description": "The index of the file, Example \"2\" for greeting 2. In create (POST) requests index cab be omitted and the API will select a new index automatically."
                },
                "script": {
                      "type": "string",
                      "description": "This is a simple explanation of what the audio is and can include what is said in the audio file for explanation."
                },
                "voice_language": {
                      "type": "string",
                      "description": "This is the language that can be requested when using TTS feature. Leave of to have API lookup configs from the defaults or domain specific configs. Default: \"en-US\"",
                      "default": "en-US"
                },
                "voice_id": {
                      "type": "string",
                      "description": "This value will allow you to select the voice to be used for TTS applications. Value must be valid or will cause errors. leave off request to have the API lookup defaults from system and/or domain. Default: \"en-US-Wavenet-C\"",
                      "default": "en-US-Wavenet-C"
                }
          },
          "required": [
                "domain",
                "synchronous",
                "script"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['synchronous', 'index', 'script', 'voice_language', 'voice_id'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/moh',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const get_domains_by_domain_moh_count: ToolDefinition = {
  schema: {
    name: 'get_domains_by_domain_moh_count',
    description: 'Count MOH for Domain — [GET /domains/{domain}/moh/count]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                }
          },
          "required": [
                "domain"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/domains/{domain}/moh/count',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const update_moh_domain_tts: ToolDefinition = {
  schema: {
    name: 'update_moh_domain_tts',
    description: 'Update MOH for Domain from TTS — [PUT /domains/{domain}/moh/{index}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "index": {
                      "type": "string",
                      "description": "index"
                },
                "synchronous": {
                      "type": "string",
                      "description": "When synchronous is requested with \"yes\" the response will be a 200 on success and will contain a valid JSON representiation of the new resource. If no or left off request a 202 \"Accepted\" will be given in a success case and this can be considered a success. Synchronous responses will be a little slower as the API will process the geo replication request and wait till the local copy has been written and can be read back berfore sending a response. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "script": {
                      "type": "string",
                      "description": "This is a simple explanation of what the audio is and can include what is said in the audio file for explanation."
                },
                "voice_language": {
                      "type": "string",
                      "description": "This is the language that can be requested when using TTS feature. Leave of to have API lookup configs from the defaults or domain specific configs. Default: \"en-US\"",
                      "default": "en-US"
                },
                "voice_id": {
                      "type": "string",
                      "description": "This value will allow you to select the voice to be used for TTS applications. Value must be valid or will cause errors. leave off request to have the API lookup defaults from system and/or domain. Default: \"en-US-Wavenet-C\"",
                      "default": "en-US-Wavenet-C"
                }
          },
          "required": [
                "domain",
                "index",
                "synchronous",
                "script"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'index'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['synchronous', 'script', 'voice_language', 'voice_id'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/domains/{domain}/moh/{index}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const delete_moh_domain: ToolDefinition = {
  schema: {
    name: 'delete_moh_domain',
    description: 'Delete MOH for Domain — [DELETE /domains/{domain}/moh/{index}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "index": {
                      "type": "string",
                      "description": "index"
                }
          },
          "required": [
                "domain",
                "index"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'index'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'DELETE',
      pathTemplate: '/domains/{domain}/moh/{index}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const create_moh_domain_base64: ToolDefinition = {
  schema: {
    name: 'create_moh_domain_base64',
    description: 'Create a new MOH for Domain from Upload (JSON + Base64 File) — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [POST /domains/{domain}/moh]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "synchronous": {
                      "type": "string",
                      "description": "When synchronous is requested with \"yes\" the response will be a 200 on success and will contain a valid JSON representiation of the new resource. If no or left off request a 202 \"Accepted\" will be given in a success case and this can be considered a success. Synchronous responses will be a little slower as the API will process the geo replication request and wait till the local copy has been written and can be read back berfore sending a response. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "index": {
                      "type": "number",
                      "description": "The index of the file, Example \"2\" for greeting 2. In create (POST) requests index cab be omitted and the API will select a new index automatically."
                },
                "script": {
                      "type": "string",
                      "description": "This is a simple explanation of what the audio is and can include what is said in the audio file for explanation."
                },
                "convert": {
                      "type": "string",
                      "description": "Setting yes will allow the API to attempt to convert the file from accepted formats into the correct format for media playback. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "encoding": {
                      "type": "string",
                      "description": "Used when providing base64 encoded data to give the content tupe of the file."
                },
                "base64_file": {
                      "type": "string",
                      "description": "This is a base64 encoded string formated without line breaks or spaces."
                }
          },
          "required": [
                "domain",
                "synchronous",
                "script"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['synchronous', 'index', 'script', 'convert', 'encoding', 'base64_file'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/moh',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const update_moh_domain_base64: ToolDefinition = {
  schema: {
    name: 'update_moh_domain_base64',
    description: 'Update MOH for Domain from Upload (JSON + Base64 File) — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [PUT /domains/{domain}/moh/{index}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "index": {
                      "type": "string",
                      "description": "index"
                },
                "synchronous": {
                      "type": "string",
                      "description": "When synchronous is requested with \"yes\" the response will be a 200 on success and will contain a valid JSON representiation of the new resource. If no or left off request a 202 \"Accepted\" will be given in a success case and this can be considered a success. Synchronous responses will be a little slower as the API will process the geo replication request and wait till the local copy has been written and can be read back berfore sending a response. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "script": {
                      "type": "string",
                      "description": "This is a simple explanation of what the audio is and can include what is said in the audio file for explanation."
                },
                "convert": {
                      "type": "string",
                      "description": "Setting yes will allow the API to attempt to convert the file from accepted formats into the correct format for media playback. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "encoding": {
                      "type": "string",
                      "description": "Used when providing base64 encoded data to give the content tupe of the file."
                },
                "base64_file": {
                      "type": "string",
                      "description": "This is a base64 encoded string formated without line breaks or spaces."
                }
          },
          "required": [
                "domain",
                "index",
                "synchronous",
                "script"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'index'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['synchronous', 'script', 'convert', 'encoding', 'base64_file'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/domains/{domain}/moh/{index}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const create_moh_domain_file_upload: ToolDefinition = {
  schema: {
    name: 'create_moh_domain_file_upload',
    description: 'Create a new MOH for Domain from Upload (Multipart/Mixed Post) — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [POST /domains/{domain}/moh]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "script": {
                      "type": "string",
                      "description": "This is a simple explanation of what the audio is and can include what is said in the audio file for explanation."
                },
                "File": {
                      "type": "string",
                      "description": "This is a \"File\" type parameter that needs to include the audio file in question using a file post method."
                },
                "index": {
                      "type": "number",
                      "description": "The index of the file, Example \"2\" for greeting 2. \"0\" will be for intro message that will only be played once."
                },
                "convert": {
                      "type": "string",
                      "description": "Setting yes will allow the API to attempt to convert the file from accepted formats into the correct format for media playback"
                },
                "synchronous": {
                      "type": "string",
                      "description": "When synchronous is requested with \"yes\" the response will be a 200 on success and will contain a valid JSON representiation of the new resource. If no or left off request a 202 \"Accepted\" will be given in a success case and this can be considered a success. Synchronous responses will be a little slower as the API will process the geo replication request and wait till the local copy has been written and can be read back berfore sending a response."
                }
          },
          "required": [
                "domain",
                "synchronous"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['script', 'File', 'index', 'convert', 'synchronous'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/moh',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const update_moh_domain_file_upload: ToolDefinition = {
  schema: {
    name: 'update_moh_domain_file_upload',
    description: 'Update MOH for Domain from Upload (Multipart/Mixed Post) — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [PUT /domains/{domain}/moh/{index}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "index": {
                      "type": "string",
                      "description": "index"
                },
                "script": {
                      "type": "string",
                      "description": "This is a simple explanation of what the audio is and can include what is said in the audio file for explanation."
                },
                "File": {
                      "type": "string",
                      "description": "This is a \"File\" type parameter that needs to include the audio file in question using a file post method."
                },
                "convert": {
                      "type": "string",
                      "description": "Setting yes will allow the API to attempt to convert the file from accepted formats into the correct format for media playback"
                },
                "synchronous": {
                      "type": "string",
                      "description": "When synchronous is requested with \"yes\" the response will be a 200 on success and will contain a valid JSON representiation of the new resource. If no or left off request a 202 \"Accepted\" will be given in a success case and this can be considered a success. Synchronous responses will be a little slower as the API will process the geo replication request and wait till the local copy has been written and can be read back berfore sending a response."
                }
          },
          "required": [
                "domain",
                "index",
                "synchronous"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'index'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['script', 'File', 'convert', 'synchronous'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/domains/{domain}/moh/{index}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};
