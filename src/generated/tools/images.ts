// Auto-generated by scripts/generate-tools.ts — DO NOT EDIT
import type { ToolDefinition, GenericApiClient } from '../types.js';
import type { CallToolResult } from '@modelcontextprotocol/sdk/types.js';

export const read_image: ToolDefinition = {
  schema: {
    name: 'read_image',
    description: 'Read Image — Read an image file — [GET /images/{filename}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "filename": {
                      "type": "string",
                      "description": "The name of the image file."
                },
                "reseller": {
                      "type": "string",
                      "description": "reseller"
                },
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\"",
                      "default": "~"
                },
                "server": {
                      "type": "string",
                      "description": "server"
                }
          },
          "required": [
                "filename",
                "reseller",
                "domain",
                "server"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['filename'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const queryParamNames = ['reseller', 'domain', 'server'];
    const queryParams: Record<string, unknown> = {};
    for (const name of queryParamNames) {
      if (args[name] !== undefined) queryParams[name] = args[name];
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/images/{filename}',
      pathParams,
      queryParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const create_image_base64: ToolDefinition = {
  schema: {
    name: 'create_image_base64',
    description: 'Create Image from Upload (JSON + Base64 File) — Create an image file — [POST /images/{filename}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "filename": {
                      "type": "string",
                      "description": "The name of the image file."
                },
                "reseller": {
                      "type": "string",
                      "description": "The reseller or territory the image file applies to. Default: \"*\"",
                      "default": "*"
                },
                "domain": {
                      "type": "string",
                      "description": "The domain the image file applies to. Default: \"*\"",
                      "default": "*"
                },
                "server": {
                      "type": "string",
                      "description": "The server the image file applies to. Default: \"*\"",
                      "default": "*"
                },
                "description": {
                      "type": "string",
                      "description": "A description of the image file to indicate what it is used for"
                },
                "filetype": {
                      "type": "string",
                      "description": "Encoding (content type) of the image file"
                },
                "base64_file": {
                      "type": "string",
                      "description": "A base64 encoded string formatted without line breaks or spaces."
                }
          },
          "required": [
                "filename",
                "filetype",
                "base64_file"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['filename'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['reseller', 'domain', 'server', 'description', 'filetype', 'base64_file'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/images/{filename}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const update_image_file_upload: ToolDefinition = {
  schema: {
    name: 'update_image_file_upload',
    description: 'Update Image from Upload (Multipart/Mixed Post) — Update an image file — [PUT /images/{filename}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "filename": {
                      "type": "string",
                      "description": "The name of the image file."
                },
                "reseller": {
                      "type": "string",
                      "description": "The reseller or territory this image file applies to. Defaults to \"*\""
                },
                "domain": {
                      "type": "string",
                      "description": "The domain this image file applies to. Defaults to \"*\""
                },
                "server": {
                      "type": "string",
                      "description": "The domain this image file applies to. Defaults to \"*\""
                },
                "description": {
                      "type": "string",
                      "description": "A description of the image file to indicate what it is used for."
                },
                "File": {
                      "type": "string",
                      "description": "A \"File\" type parameter that needs to include the image file in question using a file post method."
                }
          },
          "required": [
                "filename",
                "File"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['filename'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['reseller', 'domain', 'server', 'description', 'File'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/images/{filename}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const delete_image: ToolDefinition = {
  schema: {
    name: 'delete_image',
    description: 'Delete an Image — Delete an image file — [DELETE /images/{filename}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "filename": {
                      "type": "string",
                      "description": "The name of the image file."
                },
                "reseller": {
                      "type": "string",
                      "description": "The reseller or territory the image file applies to. Default: \"*\"",
                      "default": "*"
                },
                "domain": {
                      "type": "string",
                      "description": "The domain the image file applies to. Default: \"*\"",
                      "default": "*"
                },
                "server": {
                      "type": "string",
                      "description": "The server the image file applies to. Default: \"*\"",
                      "default": "*"
                }
          },
          "required": [
                "filename"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['filename'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['reseller', 'domain', 'server'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'DELETE',
      pathTemplate: '/images/{filename}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const create_image_file_upload: ToolDefinition = {
  schema: {
    name: 'create_image_file_upload',
    description: 'Create Image from Upload (Multipart/Mixed Post) — Create an image file — [POST /images/{filename}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "filename": {
                      "type": "string",
                      "description": "The name of the image file."
                },
                "reseller": {
                      "type": "string",
                      "description": "The reseller or territory this image file applies to. Defaults to \"*\""
                },
                "domain": {
                      "type": "string",
                      "description": "The domain this image file applies to. Defaults to \"*\""
                },
                "server": {
                      "type": "string",
                      "description": "The server this image file applies to. Defaults to \"*\""
                },
                "description": {
                      "type": "string",
                      "description": "A description of the image to indicate what it is used for"
                },
                "File": {
                      "type": "string",
                      "description": "A \"File\" type parameter that needs to include the image file in question using a file post method"
                }
          },
          "required": [
                "filename",
                "File"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['filename'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['reseller', 'domain', 'server', 'description', 'File'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/images/{filename}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const update_image_base64: ToolDefinition = {
  schema: {
    name: 'update_image_base64',
    description: 'Update Image from Upload (JSON + Base64 File) — Update an image file — [PUT /images/{filename}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "filename": {
                      "type": "string",
                      "description": "The name of the image file."
                },
                "reseller": {
                      "type": "string",
                      "description": "The reseller or territory the image file applies to. Default: \"*\"",
                      "default": "*"
                },
                "domain": {
                      "type": "string",
                      "description": "The domain the image file applies to. Default: \"*\"",
                      "default": "*"
                },
                "server": {
                      "type": "string",
                      "description": "The server the image file applies to. Default: \"*\"",
                      "default": "*"
                },
                "description": {
                      "type": "string",
                      "description": "A description of the image file to indicate what it is used for"
                },
                "filetype": {
                      "type": "string",
                      "description": "Encoding (content type) of the image file"
                },
                "base64_file": {
                      "type": "string",
                      "description": "A base64 encoded string formatted without line breaks or spaces."
                }
          },
          "required": [
                "filename",
                "filetype",
                "base64_file"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['filename'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['reseller', 'domain', 'server', 'description', 'filetype', 'base64_file'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/images/{filename}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};
