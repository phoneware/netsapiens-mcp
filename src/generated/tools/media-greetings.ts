// Auto-generated by scripts/generate-tools.ts — DO NOT EDIT
import type { ToolDefinition, GenericApiClient } from '../types.js';
import type { CallToolResult } from '@modelcontextprotocol/sdk/types.js';

export const read_greetings: ToolDefinition = {
  schema: {
    name: 'read_greetings',
    description: 'Read Greetings for User — [GET /domains/{domain}/users/{user}/greetings]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                }
          },
          "required": [
                "domain",
                "user"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/domains/{domain}/users/{user}/greetings',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const create_greeting_tts: ToolDefinition = {
  schema: {
    name: 'create_greeting_tts',
    description: 'Create a new Greeting from TTS — [POST /domains/{domain}/users/{user}/greetings]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "synchronous": {
                      "type": "string",
                      "description": "When synchronous is requested with \"yes\" the response will be a 200 on success and will contain a valid JSON representiation of the new resource. If no or left off request a 202 \"Accepted\" will be given in a success case and this can be considered a success. Synchronous responses will be a little slower as the API will process the geo replication request and wait till the local copy has been written and can be read back berfore sending a response. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "index": {
                      "type": "number",
                      "description": "The index of the file, Example \"2\" for greeting 2. In create (POST) requests index cab be omitted and the API will select a new index automatically."
                },
                "script": {
                      "type": "string",
                      "description": "This is a simple explanation of what the audio is and can include what is said in the audio file for explanation."
                },
                "voice_language": {
                      "type": "string",
                      "description": "This is the language that can be requested when using TTS feature. Leave of to have API lookup configs from the defaults or domain specific configs. Default: \"en-US\"",
                      "default": "en-US"
                },
                "voice_id": {
                      "type": "string",
                      "description": "This value will allow you to select the voice to be used for TTS applications. Value must be valid or will cause errors. leave off request to have the API lookup defaults from system and/or domain. Default: \"en-US-Wavenet-C\"",
                      "default": "en-US-Wavenet-C"
                }
          },
          "required": [
                "domain",
                "user",
                "synchronous",
                "script"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['synchronous', 'index', 'script', 'voice_language', 'voice_id'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/users/{user}/greetings',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const get_domains_by_domain_users_by_user_greetings_count: ToolDefinition = {
  schema: {
    name: 'get_domains_by_domain_users_by_user_greetings_count',
    description: 'Count Greetings for User — [GET /domains/{domain}/users/{user}/greetings/count]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                }
          },
          "required": [
                "domain",
                "user"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/domains/{domain}/users/{user}/greetings/count',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const read_greeting: ToolDefinition = {
  schema: {
    name: 'read_greeting',
    description: 'Read Specific Greeting for User — [GET /domains/{domain}/users/{user}/greetings/{index}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "index": {
                      "type": "string",
                      "description": "index"
                }
          },
          "required": [
                "domain",
                "user",
                "index"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'index'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/domains/{domain}/users/{user}/greetings/{index}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const update_greeting_tts: ToolDefinition = {
  schema: {
    name: 'update_greeting_tts',
    description: 'Update Greeting with TTS script — [PUT /domains/{domain}/users/{user}/greetings/{index}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "index": {
                      "type": "string",
                      "description": "index"
                },
                "synchronous": {
                      "type": "string",
                      "description": "When synchronous is requested with \"yes\" the response will be a 200 on success and will contain a valid JSON representiation of the new resource. If no or left off request a 202 \"Accepted\" will be given in a success case and this can be considered a success. Synchronous responses will be a little slower as the API will process the geo replication request and wait till the local copy has been written and can be read back berfore sending a response. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "script": {
                      "type": "string",
                      "description": "This is a simple explanation of what the audio is and can include what is said in the audio file for explanation."
                },
                "convert": {
                      "type": "string",
                      "description": "Setting yes will allow the API to attempt to convert the file from accepted formats into the correct format for media playback. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "encoding": {
                      "type": "string",
                      "description": "Used when providing base64 encoded data to give the content tupe of the file."
                },
                "base64_file": {
                      "type": "string",
                      "description": "This is a base64 encoded string formated without line breaks or spaces."
                },
                "voice_language": {
                      "type": "string",
                      "description": "This is the language that can be requested when using TTS feature. Leave of to have API lookup configs from the defaults or domain specific configs. Default: \"en-US\"",
                      "default": "en-US"
                },
                "voice_id": {
                      "type": "string",
                      "description": "This value will allow you to select the voice to be used for TTS applications. Value must be valid or will cause errors. leave off request to have the API lookup defaults from system and/or domain. Default: \"en-US-Wavenet-C\"",
                      "default": "en-US-Wavenet-C"
                }
          },
          "required": [
                "domain",
                "user",
                "index",
                "synchronous",
                "script"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'index'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['synchronous', 'script', 'convert', 'encoding', 'base64_file', 'voice_language', 'voice_id'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/domains/{domain}/users/{user}/greetings/{index}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const delete_greeting: ToolDefinition = {
  schema: {
    name: 'delete_greeting',
    description: 'Delete Specific Greeting for User — [DELETE /domains/{domain}/users/{user}/greetings/{index}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "index": {
                      "type": "string",
                      "description": "index"
                }
          },
          "required": [
                "domain",
                "user",
                "index"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'index'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'DELETE',
      pathTemplate: '/domains/{domain}/users/{user}/greetings/{index}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const create_greeting_base64: ToolDefinition = {
  schema: {
    name: 'create_greeting_base64',
    description: 'Create a new Greeting from Upload (JSON + Base64 File) — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [POST /domains/{domain}/users/{user}/greetings]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "synchronous": {
                      "type": "string",
                      "description": "When synchronous is requested with \"yes\" the response will be a 200 on success and will contain a valid JSON representiation of the new resource. If no or left off request a 202 \"Accepted\" will be given in a success case and this can be considered a success. Synchronous responses will be a little slower as the API will process the geo replication request and wait till the local copy has been written and can be read back berfore sending a response. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "index": {
                      "type": "number",
                      "description": "The index of the file, Example \"2\" for greeting 2. In create (POST) requests index cab be omitted and the API will select a new index automatically."
                },
                "script": {
                      "type": "string",
                      "description": "This is a simple explanation of what the audio is and can include what is said in the audio file for explanation."
                },
                "convert": {
                      "type": "string",
                      "description": "Setting yes will allow the API to attempt to convert the file from accepted formats into the correct format for media playback. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "encoding": {
                      "type": "string",
                      "description": "Used when providing base64 encoded data to give the content tupe of the file."
                },
                "base64_file": {
                      "type": "string",
                      "description": "This is a base64 encoded string formated without line breaks or spaces."
                },
                "voice_language": {
                      "type": "string",
                      "description": "This is the language that can be requested when using TTS feature. Leave of to have API lookup configs from the defaults or domain specific configs. Default: \"en-US\"",
                      "default": "en-US"
                },
                "voice_id": {
                      "type": "string",
                      "description": "This value will allow you to select the voice to be used for TTS applications. Value must be valid or will cause errors. leave off request to have the API lookup defaults from system and/or domain. Default: \"en-US-Wavenet-C\"",
                      "default": "en-US-Wavenet-C"
                }
          },
          "required": [
                "domain",
                "user",
                "synchronous",
                "script"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['synchronous', 'index', 'script', 'convert', 'encoding', 'base64_file', 'voice_language', 'voice_id'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/users/{user}/greetings',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const update_greeting_base64: ToolDefinition = {
  schema: {
    name: 'update_greeting_base64',
    description: 'Update Greeting from Upload (JSON + Base64 File) — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [PUT /domains/{domain}/users/{user}/greetings/{index}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "index": {
                      "type": "number",
                      "description": "The index of the file, Example \"2\" for greeting 2. In create (POST) requests index cab be omitted and the API will select a new index automatically."
                },
                "synchronous": {
                      "type": "string",
                      "description": "When synchronous is requested with \"yes\" the response will be a 200 on success and will contain a valid JSON representiation of the new resource. If no or left off request a 202 \"Accepted\" will be given in a success case and this can be considered a success. Synchronous responses will be a little slower as the API will process the geo replication request and wait till the local copy has been written and can be read back berfore sending a response. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "script": {
                      "type": "string",
                      "description": "This is a simple explanation of what the audio is and can include what is said in the audio file for explanation."
                },
                "convert": {
                      "type": "string",
                      "description": "Setting yes will allow the API to attempt to convert the file from accepted formats into the correct format for media playback. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "encoding": {
                      "type": "string",
                      "description": "Used when providing base64 encoded data to give the content tupe of the file."
                },
                "base64_file": {
                      "type": "string",
                      "description": "This is a base64 encoded string formated without line breaks or spaces."
                },
                "voice_language": {
                      "type": "string",
                      "description": "This is the language that can be requested when using TTS feature. Leave of to have API lookup configs from the defaults or domain specific configs. Default: \"en-US\"",
                      "default": "en-US"
                },
                "voice_id": {
                      "type": "string",
                      "description": "This value will allow you to select the voice to be used for TTS applications. Value must be valid or will cause errors. leave off request to have the API lookup defaults from system and/or domain. Default: \"en-US-Wavenet-C\"",
                      "default": "en-US-Wavenet-C"
                }
          },
          "required": [
                "domain",
                "user",
                "index",
                "synchronous",
                "script"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'index'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['synchronous', 'index', 'script', 'convert', 'encoding', 'base64_file', 'voice_language', 'voice_id'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/domains/{domain}/users/{user}/greetings/{index}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const create_greeting_file_upload: ToolDefinition = {
  schema: {
    name: 'create_greeting_file_upload',
    description: 'Create a new Greeting from Upload (Multipart/Mixed Post) — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [POST /domains/{domain}/users/{user}/greetings]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "script": {
                      "type": "string",
                      "description": "This is a simple explanation of what the audio is and can include what is said in the audio file for explanation."
                },
                "File": {
                      "type": "string",
                      "description": "This is a \"File\" type parameter that needs to include the audio file in question using a file post method."
                },
                "index": {
                      "type": "number",
                      "description": "The index of the file, Example \"2\" for greeting 2."
                },
                "convert": {
                      "type": "string",
                      "description": "Setting yes will allow the API to attempt to convert the file from accepted formats into the correct format for media playback"
                },
                "synchronous": {
                      "type": "string",
                      "description": "When synchronous is requested with \"yes\" the response will be a 200 on success and will contain a valid JSON representiation of the new resource. If no or left off request a 202 \"Accepted\" will be given in a success case and this can be considered a success. Synchronous responses will be a little slower as the API will process the geo replication request and wait till the local copy has been written and can be read back berfore sending a response."
                }
          },
          "required": [
                "domain",
                "user",
                "synchronous"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['script', 'File', 'index', 'convert', 'synchronous'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/users/{user}/greetings',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const update_greeting_file_upload: ToolDefinition = {
  schema: {
    name: 'update_greeting_file_upload',
    description: 'Update Greeting from Upload (Multipart/Mixed Post) — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [PUT /domains/{domain}/users/{user}/greetings/{index}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "index": {
                      "type": "string",
                      "description": "index"
                },
                "script": {
                      "type": "string",
                      "description": "This is a simple explanation of what the audio is and can include what is said in the audio file for explanation."
                },
                "File": {
                      "type": "string",
                      "description": "This is a \"File\" type parameter that needs to include the audio file in question using a file post method."
                },
                "convert": {
                      "type": "string",
                      "description": "Setting yes will allow the API to attempt to convert the file from accepted formats into the correct format for media playback"
                },
                "synchronous": {
                      "type": "string",
                      "description": "When synchronous is requested with \"yes\" the response will be a 200 on success and will contain a valid JSON representiation of the new resource. If no or left off request a 202 \"Accepted\" will be given in a success case and this can be considered a success. Synchronous responses will be a little slower as the API will process the geo replication request and wait till the local copy has been written and can be read back berfore sending a response."
                }
          },
          "required": [
                "domain",
                "user",
                "index"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'index'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['script', 'File', 'convert', 'synchronous'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/domains/{domain}/users/{user}/greetings/{index}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};
