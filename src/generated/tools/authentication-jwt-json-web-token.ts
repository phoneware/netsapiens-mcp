// Auto-generated by scripts/generate-tools.ts — DO NOT EDIT
import type { ToolDefinition, GenericApiClient } from '../types.js';
import type { CallToolResult } from '@modelcontextprotocol/sdk/types.js';

export const get_jwt: ToolDefinition = {
  schema: {
    name: 'get_jwt',
    description: 'Read Current JWT — [GET /jwt]',
    inputSchema: {
          "type": "object",
          "properties": {}
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/jwt',
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_jwt_1: ToolDefinition = {
  schema: {
    name: 'post_jwt_1',
    description: 'Create JWT token From User/Pass — This API will accept username and password and generate a JWT token. — [POST /jwt]',
    inputSchema: {
          "type": "object",
          "properties": {
                "grant_type": {
                      "type": "string",
                      "description": "This is the type of authentication request being made. 'mfa' would be needed in the second request with MFA passcode. Allowed values: \"mfa\", \"password\", \"refresh_token\". Default: \"mfa\"",
                      "enum": [
                            "mfa",
                            "password",
                            "refresh_token"
                      ],
                      "default": "mfa"
                },
                "client_id": {
                      "type": "string",
                      "description": "This is the client_id provided to the developer for accessing the api. Will match with the client_secret"
                },
                "client_secret": {
                      "type": "string",
                      "description": "This is the client_secret provided to the developer for accessing the api. Will match with the client_id"
                },
                "username": {
                      "type": "string",
                      "description": "This is the username and logic for the user. This may look like a uid (user@domain) or could be something else if setup diffferent including a email address potentially."
                },
                "password": {
                      "type": "string",
                      "description": "This is the password for the user requesting access."
                }
          },
          "required": [
                "grant_type",
                "client_id",
                "client_secret",
                "username",
                "password"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['grant_type', 'client_id', 'client_secret', 'username', 'password'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/jwt',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const revoke_my_jwt: ToolDefinition = {
  schema: {
    name: 'revoke_my_jwt',
    description: 'Revoke current JWT  — This API requires a valid JWT and will revoke it by JTI of the current Token. It will then prevent its use moving forward. — [DELETE /jwt]',
    inputSchema: {
          "type": "object",
          "properties": {}
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const response = await client.request({
      method: 'DELETE',
      pathTemplate: '/jwt',
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_jwt_2: ToolDefinition = {
  schema: {
    name: 'post_jwt_2',
    description: 'Create JWT token from Refresh JWT — This API will accept a refresh token in JSON object to grant a new JWT and revoke the refresh token as used.   > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [POST /jwt]',
    inputSchema: {
          "type": "object",
          "properties": {
                "refresh": {
                      "type": "string",
                      "description": "This is a refresh JWT that was recieved when creating a new JWT. it will be revoked once used and a new one given."
                }
          },
          "required": [
                "refresh"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['refresh'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/jwt',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_jwt_3: ToolDefinition = {
  schema: {
    name: 'post_jwt_3',
    description: 'Create JWT token after MFA request — This API will accept username and password along with a passcode generated via a authenticator application and generate a JWT token. Please recall that on the endpoint notation here in the docs, the "#2" after "/jwt" should be removed and is only there to facilitate naming. — [POST /jwt]',
    inputSchema: {
          "type": "object",
          "properties": {
                "grant_type": {
                      "type": "string",
                      "description": "This is the type of authentication request being made. 'mfa' would be needed in the second request with MFA passcode. Allowed values: \"mfa\", \"password\", \"refresh_token\". Default: \"mfa\"",
                      "enum": [
                            "mfa",
                            "password",
                            "refresh_token"
                      ],
                      "default": "mfa"
                },
                "client_id": {
                      "type": "string",
                      "description": "This is the client_id provided to the developer for accessing the api. Will match with the client_secret"
                },
                "client_secret": {
                      "type": "string",
                      "description": "This is the client_secret provided to the developer for accessing the api. Will match with the client_id"
                },
                "username": {
                      "type": "string",
                      "description": "This is the username and logic for the user. This may look like a uid (user@domain) or could be something else if setup diffferent including a email address potentially."
                },
                "password": {
                      "type": "string",
                      "description": "This is the password for the user requesting access."
                },
                "mfa_type": {
                      "type": "string",
                      "description": "This value will be provided in response to a auth request if MFA is needed. Move value from that token to this request. Likely will be 'authenticator'. Default: \"authenticator\"",
                      "default": "authenticator"
                },
                "mfa_vendor": {
                      "type": "string",
                      "description": "This value will be provided in response to a auth request if MFA is needed. Move value from that token to this request.  Likely will be 'google'. Default: \"google\"",
                      "default": "google"
                },
                "ns_id_type": {
                      "type": "string",
                      "description": "Will likely be subscriber as the value here to defierentiate between authenticator for api/portal and mfa with admin ui. Allowed values: \"subscriber\", \"level1\". Default: \"subscriber\"",
                      "enum": [
                            "subscriber",
                            "level1"
                      ],
                      "default": "subscriber"
                },
                "passcode": {
                      "type": "string",
                      "description": "This will be the 6 digit numberic passcode provided by a"
                },
                "access_token": {
                      "type": "string",
                      "description": "This will be the access_token or jwt that was given with the scope of mfa_required"
                }
          },
          "required": [
                "grant_type",
                "client_id",
                "client_secret",
                "username",
                "password",
                "mfa_type",
                "mfa_vendor",
                "ns_id_type",
                "passcode",
                "access_token"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['grant_type', 'client_id', 'client_secret', 'username', 'password', 'mfa_type', 'mfa_vendor', 'ns_id_type', 'passcode', 'access_token'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/jwt',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_jwt_4: ToolDefinition = {
  schema: {
    name: 'post_jwt_4',
    description: 'Create JWT token For Delegated  Access — This API will require valid access through APIKey and allows for generation of a JWT for a different user.   > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [POST /jwt]',
    inputSchema: {
          "type": "object",
          "properties": {
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account."
                },
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. ~ and * can be used alone in special cases to mean My Domain (~) and All Domains (\\*)."
                }
          },
          "required": [
                "user",
                "domain"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['user', 'domain'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/jwt',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const revoke_jwtby_uid: ToolDefinition = {
  schema: {
    name: 'revoke_jwtby_uid',
    description: 'Revoke JWT(s) by UID (user@domain) — [DELETE /jwt/{uid}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "uid": {
                      "type": "string",
                      "description": "uid"
                }
          },
          "required": [
                "uid"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['uid'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'DELETE',
      pathTemplate: '/jwt/{uid}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const revoke_jwtby_jti: ToolDefinition = {
  schema: {
    name: 'revoke_jwtby_jti',
    description: 'Revoke JWT by JTI (JWT ID) — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [DELETE /jwt/{jti}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "jti": {
                      "type": "string",
                      "description": "jti"
                }
          },
          "required": [
                "jti"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['jti'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'DELETE',
      pathTemplate: '/jwt/{jti}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};
