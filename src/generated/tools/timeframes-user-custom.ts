// Auto-generated by scripts/generate-tools.ts — DO NOT EDIT
import type { ToolDefinition, GenericApiClient } from '../types.js';
import type { CallToolResult } from '@modelcontextprotocol/sdk/types.js';

export const post_domains_by_domain_users_by_user_timeframes_by_id_3: ToolDefinition = {
  schema: {
    name: 'post_domains_by_domain_users_by_user_timeframes_by_id_3',
    description: 'Create Additional Entries within Custom Timeframe — When creating additional entries within a Custom timeframe it is not necessary to supply a ```timeframe-id``` for the new entries. These ```timeframe-id```s will be generated automatically. — [POST /domains/{domain}/users/{user}/timeframes/{id}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "id": {
                      "type": "string",
                      "description": "```timeframe-id``` of the timeframe to modify"
                },
                "timeframe-specific-dates-array": {
                      "type": "array",
                      "description": "timeframe-specific-dates-array"
                },
                "timeframe-holiday-array": {
                      "type": "array",
                      "description": "timeframe-holiday-array"
                },
                "timeframe-days-of-week-array": {
                      "type": "array",
                      "description": "timeframe-days-of-week-array"
                }
          },
          "required": [
                "domain",
                "user",
                "id"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'id'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['timeframe-specific-dates-array', 'timeframe-holiday-array', 'timeframe-days-of-week-array'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/users/{user}/timeframes/{id}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const delete_domains_by_domain_users_by_user_timeframes_by_id_3: ToolDefinition = {
  schema: {
    name: 'delete_domains_by_domain_users_by_user_timeframes_by_id_3',
    description: 'Delete Entry within Custom Timeframe — In order to delete an entry within a Custom timeframe, supply the ```timeframe-id``` of the particular entry to be deleted as the parameter ```child_id``` within the request. — [DELETE /domains/{domain}/users/{user}/timeframes/{id}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "id": {
                      "type": "string",
                      "description": "```timeframe-id``` of the timeframe to modify"
                },
                "child_id": {
                      "type": "string",
                      "description": "The ```id``` of the entry you wish to delete within the timeframe."
                }
          },
          "required": [
                "domain",
                "user",
                "id",
                "child_id"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'id'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['child_id'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'DELETE',
      pathTemplate: '/domains/{domain}/users/{user}/timeframes/{id}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_domains_by_domain_users_by_user_timeframes_5: ToolDefinition = {
  schema: {
    name: 'post_domains_by_domain_users_by_user_timeframes_5',
    description: 'Create Custom Timeframe — When creating a Custom timeframe it is not necessary to supply a ```timeframe-id``` for the timeframe or for the entries within that timeframe. These ```timeframe-id```s will be generated automatically. — [POST /domains/{domain}/users/{user}/timeframes]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "synchronous": {
                      "type": "string",
                      "description": "When synchronous is requested with \"yes\" the response will be a 200 on success and will contain a valid JSON representiation of the new resource. If no or left off request a 202 \"Accepted\" will be given in a success case and this can be considered a success. Synchronous responses will be a little slower as the API will process the geo replication request and wait till the local copy has been written and can be read back berfore sending a response. Allowed values: \"yes\", \"no\". Default: \"no\"",
                      "enum": [
                            "yes",
                            "no"
                      ],
                      "default": "no"
                },
                "timeframe-name": {
                      "type": "string",
                      "description": "timeframe-name"
                },
                "timeframe-type": {
                      "type": "string",
                      "description": "```days-of-week```: Days of Week ```specific-dates```: Specific Dates ```holiday```: Holidays ```custom```: Custom ```always```: Always"
                },
                "timeframe-specific-dates-array": {
                      "type": "array",
                      "description": "timeframe-specific-dates-array"
                },
                "timeframe-holiday-array": {
                      "type": "array",
                      "description": "timeframe-holiday-array"
                },
                "timeframe-days-of-week-array": {
                      "type": "array",
                      "description": "timeframe-days-of-week-array"
                }
          },
          "required": [
                "domain",
                "user",
                "synchronous"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['synchronous', 'timeframe-name', 'timeframe-type', 'timeframe-specific-dates-array', 'timeframe-holiday-array', 'timeframe-days-of-week-array'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/users/{user}/timeframes',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const put_domains_by_domain_users_by_user_timeframes_by_id_6: ToolDefinition = {
  schema: {
    name: 'put_domains_by_domain_users_by_user_timeframes_by_id_6',
    description: 'Update Entries within Custom Timeframe — In order to update entries within a Custom timeframe, include the parameter ```update_only``` and provide within the arrays ```timeframe-specific-dates-array```, ```timeframe-day-of-week-array```, and/or ```timeframe-holiday-array``` the new values for the entries you intend to update, including in each entry the associated ```timeframe-id``` for that entry. If any entries are missing the ```timeframe-id``` parameter, it may result in undesired behavior.  The value for ```update-only``` must be set to ```yes``` for this operation to succeed as desired. If any other value is supplied or the parameter ```update-only``` is omitted, it will result in the behavior described in "Replace All Entries in Custom Timeframe" - see examples for more information. — [PUT /domains/{domain}/users/{user}/timeframes/{id}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "id": {
                      "type": "string",
                      "description": "```timeframe-id``` of the timeframe to modify"
                },
                "update-only": {
                      "type": "string",
                      "description": "The value for ```update-only``` must be set to ```yes``` for this operation to succeed as desired. If any other value is supplied or the parameter ```update-only``` is omitted, undesired behavior will occur."
                },
                "timeframe-specific-dates-array": {
                      "type": "array",
                      "description": "timeframe-specific-dates-array"
                },
                "timeframe-holiday-array": {
                      "type": "array",
                      "description": "timeframe-holiday-array"
                },
                "timeframe-days-of-week-array": {
                      "type": "array",
                      "description": "timeframe-days-of-week-array"
                }
          },
          "required": [
                "domain",
                "user",
                "id",
                "update-only"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'id'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['update-only', 'timeframe-specific-dates-array', 'timeframe-holiday-array', 'timeframe-days-of-week-array'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/domains/{domain}/users/{user}/timeframes/{id}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const put_domains_by_domain_users_by_user_timeframes_by_id_7: ToolDefinition = {
  schema: {
    name: 'put_domains_by_domain_users_by_user_timeframes_by_id_7',
    description: 'Replace All Entries in Custom Timeframe — If a new array of entries is supplied in the update request for a Custom timeframe, it will replace all existing entries in the timeframe, meaning that any existing entries in that timeframe will be removed. To update individual entries within a Custom timeframe, refer to "Update Entries within Custom Timeframe." It is not necessary to supply a ```timeframe-id``` for the new holidays. These ```timeframe-id```s will be generated automatically. — [PUT /domains/{domain}/users/{user}/timeframes/{id}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "id": {
                      "type": "string",
                      "description": "```timeframe-id``` of the timeframe to modify"
                },
                "timeframe-specific-dates-array": {
                      "type": "array",
                      "description": "timeframe-specific-dates-array"
                },
                "timeframe-holiday-array": {
                      "type": "array",
                      "description": "timeframe-holiday-array"
                },
                "timeframe-days-of-week-array": {
                      "type": "array",
                      "description": "timeframe-days-of-week-array"
                }
          },
          "required": [
                "domain",
                "user",
                "id"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'id'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['timeframe-specific-dates-array', 'timeframe-holiday-array', 'timeframe-days-of-week-array'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/domains/{domain}/users/{user}/timeframes/{id}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};
