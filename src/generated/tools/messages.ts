// Auto-generated by scripts/generate-tools.ts — DO NOT EDIT
import type { ToolDefinition, GenericApiClient } from '../types.js';
import type { CallToolResult } from '@modelcontextprotocol/sdk/types.js';

export const get_message_sessions_for_domain: ToolDefinition = {
  schema: {
    name: 'get_message_sessions_for_domain',
    description: 'Get Messagesessions for Domain — [GET /domains/{domain}/messagesessions]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "limit": {
                      "type": "number",
                      "description": "This is the limit of the number of objects that will be returned in the read/list API call. This can be used with pagination to help retrieve smaller chunks of data. Should be used in conjunction with Start. Default: 100",
                      "default": 100
                }
          },
          "required": [
                "domain"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const queryParamNames = ['limit'];
    const queryParams: Record<string, unknown> = {};
    for (const name of queryParamNames) {
      if (args[name] !== undefined) queryParams[name] = args[name];
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/domains/{domain}/messagesessions',
      pathParams,
      queryParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const start_session: ToolDefinition = {
  schema: {
    name: 'start_session',
    description: 'Start a new Message Session — This is a good place to start with messaging if you do not already have a message session ID. — [POST /domains/{domain}/users/{user}/messages]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "type": {
                      "type": "string",
                      "description": "type"
                },
                "message": {
                      "type": "string",
                      "description": "This is the text that will be sent as the message"
                },
                "destination": {
                      "type": "string",
                      "description": "This is a single user or phonenumber for chat and SMS respectively. If sending to multiple recipients, then should be an array of users or phonenumbers"
                },
                "from-number": {
                      "type": "string",
                      "description": "This is used only for SMS. This is the user's number used to send outbound for SMS"
                },
                "data": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the base64 data of the media file to be sent"
                },
                "mime-type": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the mime type of the media file to be sent"
                },
                "size": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the size in bytes of the media file being sent"
                }
          },
          "required": [
                "domain",
                "user",
                "type",
                "message",
                "destination"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['type', 'message', 'destination', 'from-number', 'data', 'mime-type', 'size'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/users/{user}/messages',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const get_message_sessions_for_user: ToolDefinition = {
  schema: {
    name: 'get_message_sessions_for_user',
    description: 'Get Messagesessions for User — [GET /domains/{domain}/users/{user}/messagesessions]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "limit": {
                      "type": "number",
                      "description": "This is the limit of the number of objects that will be returned in the read/list API call. This can be used with pagination to help retrieve smaller chunks of data. Should be used in conjunction with Start. Default: 100",
                      "default": 100
                }
          },
          "required": [
                "domain",
                "user"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const queryParamNames = ['limit'];
    const queryParams: Record<string, unknown> = {};
    for (const name of queryParamNames) {
      if (args[name] !== undefined) queryParams[name] = args[name];
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/domains/{domain}/users/{user}/messagesessions',
      pathParams,
      queryParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const get_message_session_for_user: ToolDefinition = {
  schema: {
    name: 'get_message_session_for_user',
    description: 'Get Messagesession for User — [GET /domains/{domain}/users/{user}/messagesessions/{messagesession}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "messagesession": {
                      "type": "string",
                      "description": "messagesession"
                }
          },
          "required": [
                "domain",
                "user",
                "messagesession"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'messagesession'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/domains/{domain}/users/{user}/messagesessions/{messagesession}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const update_message_session_participants: ToolDefinition = {
  schema: {
    name: 'update_message_session_participants',
    description: 'Update Messagesession (Participants) — This is how you add or remove participants from a chat message session. You cannot add or remove from a group MMS session, in that case you must start a new session. — [PUT /domains/{domain}/users/{user}/messagesessions/{messagesession}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "messagesession": {
                      "type": "string",
                      "description": "ID of the message session to send in (when changing participants, do not change the session ID)"
                },
                "participants": {
                      "type": "string",
                      "description": "This is a comma separated list of User IDs that will be the final list of users in the group chat. This includes the current user's User ID"
                }
          },
          "required": [
                "domain",
                "user",
                "messagesession",
                "participants"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'messagesession'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['participants'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/domains/{domain}/users/{user}/messagesessions/{messagesession}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const delete_messagesession: ToolDefinition = {
  schema: {
    name: 'delete_messagesession',
    description: 'Delete Messagesession — [DELETE /domains/{domain}/users/{user}/messagesessions/{messagesession}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "messagesession": {
                      "type": "string",
                      "description": "ID of the message session to delete"
                }
          },
          "required": [
                "domain",
                "user",
                "messagesession"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'messagesession'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'DELETE',
      pathTemplate: '/domains/{domain}/users/{user}/messagesessions/{messagesession}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const get_messages_for_messagesession: ToolDefinition = {
  schema: {
    name: 'get_messages_for_messagesession',
    description: 'Get Messages for Messagesession — [GET /domains/{domain}/users/{user}/messagesessions/{messagesession}/messages]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "messagesession": {
                      "type": "string",
                      "description": "messagesession"
                },
                "limit": {
                      "type": "number",
                      "description": "This is the limit of the number of objects that will be returned in the read/list API call. This can be used with pagination to help retrieve smaller chunks of data. Should be used in conjunction with Start. Default: 100",
                      "default": 100
                }
          },
          "required": [
                "domain",
                "user",
                "messagesession"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'messagesession'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const queryParamNames = ['limit'];
    const queryParams: Record<string, unknown> = {};
    for (const name of queryParamNames) {
      if (args[name] !== undefined) queryParams[name] = args[name];
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/domains/{domain}/users/{user}/messagesessions/{messagesession}/messages',
      pathParams,
      queryParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const send_message_chat: ToolDefinition = {
  schema: {
    name: 'send_message_chat',
    description: 'Send a message (Chat) — [POST /domains/{domain}/users/{user}/messagesessions/{messagesession}/messages]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "messagesession": {
                      "type": "string",
                      "description": "ID of the message session to send in. Needs to be at least 32 characters long and random. Only alphanumeric chracters and underscore are allowed."
                },
                "type": {
                      "type": "string",
                      "description": "type"
                },
                "message": {
                      "type": "string",
                      "description": "This is the text that will be sent as the message"
                },
                "destination": {
                      "type": "string",
                      "description": "This is a single user or phonenumber for chat and SMS respectively. If sending to multiple recipients, then should be an array of users or phonenumbers"
                },
                "from-number": {
                      "type": "string",
                      "description": "This is used only for SMS. This is the user's number used to send outbound for SMS"
                },
                "data": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the base64 data of the media file to be sent"
                },
                "mime-type": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the mime type of the media file to be sent"
                },
                "size": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the size in bytes of the media file being sent"
                }
          },
          "required": [
                "domain",
                "user",
                "messagesession",
                "type",
                "message",
                "destination"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'messagesession'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['type', 'message', 'destination', 'from-number', 'data', 'mime-type', 'size'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/users/{user}/messagesessions/{messagesession}/messages',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const send_message_group_chat: ToolDefinition = {
  schema: {
    name: 'send_message_group_chat',
    description: 'Send a message (Group Chat) — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [POST /domains/{domain}/users/{user}/messagesessions/{messagesession}/messages]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "messagesession": {
                      "type": "string",
                      "description": "ID of the message session to send in. Needs to be at least 32 characters long and random. Only alphanumeric chracters and underscore are allowed."
                },
                "type": {
                      "type": "string",
                      "description": "type"
                },
                "message": {
                      "type": "string",
                      "description": "This is the text that will be sent as the message"
                },
                "destination": {
                      "type": "string",
                      "description": "This is a single user or phonenumber for chat and SMS respectively. If sending to multiple recipients, then should be an array of users or phonenumbers"
                },
                "from-number": {
                      "type": "string",
                      "description": "This is used only for SMS. This is the user's number used to send outbound for SMS"
                },
                "data": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the base64 data of the media file to be sent"
                },
                "mime-type": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the mime type of the media file to be sent"
                },
                "size": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the size in bytes of the media file being sent"
                }
          },
          "required": [
                "domain",
                "user",
                "messagesession",
                "type",
                "message",
                "destination"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'messagesession'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['type', 'message', 'destination', 'from-number', 'data', 'mime-type', 'size'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/users/{user}/messagesessions/{messagesession}/messages',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const send_message_media_chat: ToolDefinition = {
  schema: {
    name: 'send_message_media_chat',
    description: 'Send a message (Media Chat) — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [POST /domains/{domain}/users/{user}/messagesessions/{messagesession}/messages]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "messagesession": {
                      "type": "string",
                      "description": "ID of the message session to send in. Needs to be at least 32 characters long and random. Only alphanumeric chracters and underscore are allowed."
                },
                "type": {
                      "type": "string",
                      "description": "type"
                },
                "message": {
                      "type": "string",
                      "description": "This is the text that will be sent as the message"
                },
                "destination": {
                      "type": "string",
                      "description": "This is a single user or phonenumber for chat and SMS respectively. If sending to multiple recipients, then should be an array of users or phonenumbers"
                },
                "from-number": {
                      "type": "string",
                      "description": "This is used only for SMS. This is the user's number used to send outbound for SMS"
                },
                "data": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the base64 data of the media file to be sent"
                },
                "mime-type": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the mime type of the media file to be sent"
                },
                "size": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the size in bytes of the media file being sent"
                }
          },
          "required": [
                "domain",
                "user",
                "messagesession",
                "type",
                "message",
                "destination"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'messagesession'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['type', 'message', 'destination', 'from-number', 'data', 'mime-type', 'size'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/users/{user}/messagesessions/{messagesession}/messages',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const send_message_sms: ToolDefinition = {
  schema: {
    name: 'send_message_sms',
    description: 'Send a message (SMS) — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [POST /domains/{domain}/users/{user}/messagesessions/{messagesession}/messages]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "messagesession": {
                      "type": "string",
                      "description": "ID of the message session to send in. Needs to be at least 32 characters long and random. Only alphanumeric chracters and underscore are allowed."
                },
                "type": {
                      "type": "string",
                      "description": "type"
                },
                "message": {
                      "type": "string",
                      "description": "This is the text that will be sent as the message"
                },
                "destination": {
                      "type": "string",
                      "description": "This is a single user or phonenumber for chat and SMS respectively. If sending to multiple recipients, then should be an array of users or phonenumbers"
                },
                "from-number": {
                      "type": "string",
                      "description": "This is used only for SMS. This is the user's number used to send outbound for SMS"
                },
                "data": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the base64 data of the media file to be sent"
                },
                "mime-type": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the mime type of the media file to be sent"
                },
                "size": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the size in bytes of the media file being sent"
                }
          },
          "required": [
                "domain",
                "user",
                "messagesession",
                "type",
                "message",
                "destination"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'messagesession'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['type', 'message', 'destination', 'from-number', 'data', 'mime-type', 'size'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/users/{user}/messagesessions/{messagesession}/messages',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const send_message_group_sms: ToolDefinition = {
  schema: {
    name: 'send_message_group_sms',
    description: 'Send a message (Group SMS) — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [POST /domains/{domain}/users/{user}/messagesessions/{messagesession}/messages]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "messagesession": {
                      "type": "string",
                      "description": "ID of the message session to send in. Needs to be at least 32 characters long and random. Only alphanumeric chracters and underscore are allowed."
                },
                "type": {
                      "type": "string",
                      "description": "type"
                },
                "message": {
                      "type": "string",
                      "description": "This is the text that will be sent as the message"
                },
                "destination": {
                      "type": "string",
                      "description": "This is a single user or phonenumber for chat and SMS respectively. If sending to multiple recipients, then should be an array of users or phonenumbers"
                },
                "from-number": {
                      "type": "string",
                      "description": "This is used only for SMS. This is the user's number used to send outbound for SMS"
                },
                "data": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the base64 data of the media file to be sent"
                },
                "mime-type": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the mime type of the media file to be sent"
                },
                "size": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the size in bytes of the media file being sent"
                }
          },
          "required": [
                "domain",
                "user",
                "messagesession",
                "type",
                "message",
                "destination"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'messagesession'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['type', 'message', 'destination', 'from-number', 'data', 'mime-type', 'size'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/users/{user}/messagesessions/{messagesession}/messages',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const send_message_mms: ToolDefinition = {
  schema: {
    name: 'send_message_mms',
    description: 'Send a message (MMS) — > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [POST /domains/{domain}/users/{user}/messagesessions/{messagesession}/messages]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "messagesession": {
                      "type": "string",
                      "description": "ID of the message session to send in. Needs to be at least 32 characters long and random. Only alphanumeric chracters and underscore are allowed."
                },
                "type": {
                      "type": "string",
                      "description": "type"
                },
                "message": {
                      "type": "string",
                      "description": "This is the text that will be sent as the message"
                },
                "destination": {
                      "type": "string",
                      "description": "This is a single user or phonenumber for chat and SMS respectively. If sending to multiple recipients, then should be an array of users or phonenumbers"
                },
                "from-number": {
                      "type": "string",
                      "description": "This is used only for SMS. This is the user's number used to send outbound for SMS"
                },
                "data": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the base64 data of the media file to be sent"
                },
                "mime-type": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the mime type of the media file to be sent"
                },
                "size": {
                      "type": "string",
                      "description": "This is used only for media chat or MMS. This is the size in bytes of the media file being sent"
                }
          },
          "required": [
                "domain",
                "user",
                "messagesession",
                "type",
                "message",
                "destination"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'messagesession'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['type', 'message', 'destination', 'from-number', 'data', 'mime-type', 'size'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/domains/{domain}/users/{user}/messagesessions/{messagesession}/messages',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const update_message_session_session_name: ToolDefinition = {
  schema: {
    name: 'update_message_session_session_name',
    description: 'Update Messagesession (Session Name) — This is how you change the chat session name. You cannot name or rename an MMS group session.  > Note: # and anything after is NOT needed, its just to allow multiple examples for the same path/method. — [PUT /domains/{domain}/users/{user}/messagesessions/{messagesession}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "messagesession": {
                      "type": "string",
                      "description": "ID of the message session to send in (when changing participants, do not change the session ID)"
                },
                "session-name": {
                      "type": "string",
                      "description": "This is the new name of the message session"
                }
          },
          "required": [
                "domain",
                "user",
                "messagesession",
                "session-name"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'messagesession'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['session-name'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/domains/{domain}/users/{user}/messagesessions/{messagesession}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const leave_messagesession: ToolDefinition = {
  schema: {
    name: 'leave_messagesession',
    description: 'Update Messagesession (Leave) — This is how you leave a chat conversation. — [PUT /domains/{domain}/users/{user}/messagesessions/{messagesession}/leave]',
    inputSchema: {
          "type": "object",
          "properties": {
                "domain": {
                      "type": "string",
                      "description": "This is the main organization name. This is used to link resource to its group/tenant/organization/enterprise. Then using in path for GET you can set to \"~\" to use the domain name linked to your token/key instead of the domain name itself when reading for your own domain. Default: \"~\""
                },
                "user": {
                      "type": "string",
                      "description": "This is the main user extension for the user account. ~ can be used to mean \"my account\" which will extract the user from the token/key used for authentication. Default: \"~\""
                },
                "messagesession": {
                      "type": "string",
                      "description": "ID of the message session to send in (when changing participants, do not change the session ID)"
                }
          },
          "required": [
                "domain",
                "user",
                "messagesession"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['domain', 'user', 'messagesession'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/domains/{domain}/users/{user}/messagesessions/{messagesession}/leave',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};
