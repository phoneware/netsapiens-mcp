apiVersion: v1
kind: Secret
metadata:
  name: netsapiens-mcp
  namespace: netsapiens-mcp
type: Opaque
stringData:
  # Fill these in before applying, or use `kubectl create secret` instead:
  #   kubectl create secret generic netsapiens-mcp \
  #     --namespace netsapiens-mcp \
  #     --from-literal=NETSAPIENS_OAUTH_CLIENT_ID=your-client-id \
  #     --from-literal=NETSAPIENS_OAUTH_CLIENT_SECRET=your-client-secret
  NETSAPIENS_OAUTH_CLIENT_ID: ""
  NETSAPIENS_OAUTH_CLIENT_SECRET: ""
